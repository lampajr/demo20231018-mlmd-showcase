# demo20231018-mlmd-showcase
Showcasing the usage of ml-metadata api with gRPC

Startup the ML-Metadata C++ server
```bash
./start-mlmd.sh
```

Stop the ML-Metadata C++ server
```bash
./stop-mlmd.sh
```